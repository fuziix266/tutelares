<?php
$noticias = $this->noticias ?? [];
$uH = $this->ultimaHora;

// Filtrar las noticias para que la de última hora no se repita en el resto de la página
if ($uH) {
    $idUH = $uH['id'];
    $noticias = array_filter($noticias, fn($n) => $n['id'] != $idUH);
    $noticias = array_values($noticias); // Reindexar
}
?>

<!-- TICKER -->
<div class="ticker-bar">
    <div class="ticker-inner">
        <span>&#128308; EN VIVO</span>
        <span class="sep">|</span>
        <?php if (count($noticias) > 0): ?>
            <?php foreach (array_slice($noticias, 0, 4) as $n): ?>
                <span><?= htmlspecialchars($n['titulo']) ?></span>
                <span class="sep">&#8226;</span>
            <?php endforeach; ?>
        <?php else: ?>
            <span>No hay noticias recientes</span>
        <?php endif; ?>
    </div>
</div>

<!-- HERO -->
<section class="hero">
    <?php if ($uH): ?>
    <div class="hero-content">
        <span class="hero-tag"><i class="bi bi-lightning-charge-fill"></i> Última Hora</span>
        <h1 class="hero-title" style="font-size: clamp(2rem, 4vw, 3.5rem);">
            <?= htmlspecialchars($uH['titulo']) ?>
        </h1>
        <p class="hero-excerpt">
            <?= htmlspecialchars($uH['resumen'] ?: substr(strip_tags($uH['contenido']), 0, 180) . '...') ?>
        </p>
        <div style="display: flex; gap: 1rem; flex-wrap: wrap;">
            <a href="<?= $this->url('noticia/ver', ['id' => $uH['id']]) ?>" class="btn-hero">
                Leer noticia completa <i class="bi bi-arrow-right"></i>
            </a>
            <a href="<?= $this->url('noticia') ?>" class="btn-hero" style="background: rgba(255,255,255,0.1); color: #fff; border: 1px solid rgba(255,255,255,0.2);">
                Ver todas las noticias
            </a>
        </div>
    </div>
    <div class="hero-image">
        <img src="<?= htmlspecialchars($uH['imagen'] ?: 'https://images.unsplash.com/photo-1504711434969-e33886168f5c?w=1200&q=80') ?>" alt="<?= htmlspecialchars($uH['titulo']) ?>">
    </div>
    <?php else: ?>
    <!-- HERO DEFAULT (si no hay fijada) -->
    <div class="hero-content">
        <h1 class="hero-title">TUTELARES <span class="highlight">Noticias</span></h1>
        <p class="hero-excerpt">El portal líder en información regional. Periodismo independiente, veraz y comprometido.</p>
        <a href="<?= $this->url('noticia') ?>" class="btn-hero">Explorar Noticias <i class="bi bi-arrow-right"></i></a>
    </div>
    <div class="hero-image">
        <img src="https://images.unsplash.com/photo-1504711434969-e33886168f5c?w=1200&q=80" alt="Tutelares">
    </div>
    <?php endif; ?>
</section>

<?php if (count($noticias) > 0): ?>
<!-- NOTICIAS DESTACADAS (Portada - En Portada) -->
<section class="section container">
    <div class="section-header">
        <h2>En Portada</h2>
        <a href="<?= $this->url('noticia') ?>" class="section-link">Ver todo <i class="bi bi-arrow-right"></i></a>
    </div>

    <div class="featured-grid">
        <!-- Noticia grande -->
        <?php 
            $n0 = $noticias[0] ?? null; 
        ?>
        <?php if ($n0): ?>
        <a href="<?= $this->url('noticia/ver', ['id' => $n0['id']]) ?>" class="featured-card">
            <img src="<?= htmlspecialchars($n0['imagen'] ?: 'https://via.placeholder.com/1200x600?text=Tutelares') ?>" alt="<?= htmlspecialchars($n0['titulo']) ?>">
            <div class="featured-overlay">
                <span class="news-card-cat"><?= htmlspecialchars($n0['categoria']) ?></span>
                <?php if (($n0['fijada'] ?? 0) == 1): ?>
                    <span style="background:var(--accent);color:#fff;font-size:0.65rem;font-weight:800;padding:2px 6px;border-radius:4px;text-transform:uppercase;margin-left:0.5rem;vertical-align:middle;">
                        <i class="bi bi-lightning-charge-fill"></i> Última Hora
                    </span>
                <?php endif; ?>
                <h2 class="featured-title"><?= htmlspecialchars($n0['titulo']) ?></h2>
                <div class="news-card-meta" style="color:rgba(255,255,255,0.65);">
                    <span><i class="bi bi-person"></i> <?= htmlspecialchars($n0['autor'] ?: 'Redacción') ?></span>
                    <span><i class="bi bi-clock"></i> <?= date('d/m/Y', strtotime($n0['creado_en'])) ?></span>
                </div>
            </div>
        </a>
        <?php endif; ?>

        <!-- Noticias lateral -->
        <div class="side-cards">
            <?php foreach (array_slice($noticias, 1, 3) as $n): ?>
            <a href="<?= $this->url('noticia/ver', ['id' => $n['id']]) ?>" class="side-card">
                <div class="side-card-img" style="position: relative;">
                    <img src="<?= htmlspecialchars($n['imagen'] ?: 'https://via.placeholder.com/300x200?text=Tutelares') ?>" alt="<?= htmlspecialchars($n['titulo']) ?>">
                    <?php if (($n['fijada'] ?? 0) == 1): ?>
                        <span style="position:absolute;bottom:5px;left:5px;background:var(--accent);color:#fff;font-size:0.55rem;font-weight:800;padding:1px 4px;border-radius:3px;text-transform:uppercase;z-index:2;">
                            <i class="bi bi-lightning-charge-fill"></i> Ú. Hora
                        </span>
                    <?php endif; ?>
                </div>
                <div class="side-card-body">
                    <span class="news-card-cat" style="position:static;font-size:.65rem;"><?= htmlspecialchars($n['categoria']) ?></span>
                    <h3 class="side-card-title"><?= htmlspecialchars($n['titulo']) ?></h3>
                    <span style="font-size:.8rem;color:#999;"><i class="bi bi-clock"></i> <?= date('d M', strtotime($n['creado_en'])) ?></span>
                </div>
            </a>
            <?php endforeach; ?>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- RADIO BANNER -->
<div class="radio-banner">
    <div class="radio-info">
        <div class="radio-pulse">
            <i class="bi bi-broadcast"></i>
        </div>
        <div class="radio-text">
            <h3>Radio Tutelares • En Vivo</h3>
            <p>Transmitiendo ahora: Noticias del Mediodía — Música Andina & Regional las 24 horas</p>
        </div>
    </div>
    <a href="<?= $this->url('radio') ?>" class="btn-listen">
        <i class="bi bi-play-circle-fill"></i> Escuchar Ahora
    </a>
</div>

<!-- ÚLTIMA HORA: GRID -->
<section class="section section-alt">
    <div class="container">
        <div class="section-header">
            <h2>Últimas Noticias</h2>
            <a href="<?= $this->url('noticia') ?>" class="section-link">Más noticias <i class="bi bi-arrow-right"></i></a>
        </div>

        <div class="news-grid">
            <?php if (count($noticias) > 0): ?>
                <?php foreach ($noticias as $n): ?>
                <a href="<?= $this->url('noticia/ver', ['id' => $n['id']]) ?>" class="news-card">
                    <div class="news-card-img">
                        <img src="<?= htmlspecialchars($n['imagen'] ?: 'https://via.placeholder.com/600x400?text=Tutelares') ?>" alt="<?= htmlspecialchars($n['titulo']) ?>" loading="lazy">
                        <span class="news-card-cat"><?= htmlspecialchars($n['categoria']) ?></span>
                        <?php if (($n['fijada'] ?? 0) == 1): ?>
                            <span style="position:absolute;bottom:10px;left:10px;background:var(--accent);color:#fff;font-size:0.65rem;font-weight:800;padding:2px 6px;border-radius:4px;text-transform:uppercase;">
                                <i class="bi bi-lightning-charge-fill"></i> Última Hora
                            </span>
                        <?php endif; ?>
                    </div>
                    <div class="news-card-body">
                        <h3 class="news-card-title"><?= htmlspecialchars($n['titulo']) ?></h3>
                        <p style="font-size:.9rem;color:#666;line-height:1.6;"><?= htmlspecialchars($n['resumen'] ?: substr(strip_tags($n['contenido']), 0, 150)) ?></p>
                        <div class="news-card-meta">
                            <span><i class="bi bi-person"></i> <?= htmlspecialchars($n['autor'] ?: 'Redacción') ?></span>
                            <span><i class="bi bi-clock"></i> <?= date('d/m/Y', strtotime($n['creado_en'])) ?></span>
                        </div>
                    </div>
                </a>
                <?php endforeach; ?>
            <?php else: ?>
                <p>Próximamente más noticias...</p>
            <?php endif; ?>
        </div>
    </div>
</section>

<!-- CATEGORÍAS -->
<section class="section container">
    <div class="section-header">
        <h2>Explorar por Categoría</h2>
        <a href="<?= $this->url('categoria') ?>" class="section-link">Ver todas <i class="bi bi-arrow-right"></i></a>
    </div>
    <div class="cat-grid">
        <a href="<?= $this->url('categoria', ['id' => 'Politica']) ?>" class="cat-pill">
            <i class="bi bi-bank"></i> Política
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Economia']) ?>" class="cat-pill">
            <i class="bi bi-graph-up-arrow"></i> Economía
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Deportes']) ?>" class="cat-pill">
            <i class="bi bi-trophy"></i> Deportes
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Tecnología']) ?>" class="cat-pill">
            <i class="bi bi-cpu"></i> Tecnología
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Cultura']) ?>" class="cat-pill">
            <i class="bi bi-music-note-beamed"></i> Cultura
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Sociedad']) ?>" class="cat-pill">
            <i class="bi bi-people"></i> Sociedad
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Internacional']) ?>" class="cat-pill">
            <i class="bi bi-globe2"></i> Internacional
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Salud']) ?>" class="cat-pill">
            <i class="bi bi-heart-pulse"></i> Salud
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Religion']) ?>" class="cat-pill">
            <i class="bi bi-stars"></i> Religión
        </a>
        <a href="<?= $this->url('categoria', ['id' => 'Policial']) ?>" class="cat-pill">
            <i class="bi bi-shield-shaded"></i> Policial
        </a>
    </div>
</section>
