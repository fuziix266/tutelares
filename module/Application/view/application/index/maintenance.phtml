<?php $this->layout('layout/maintenance'); ?>

<div class="maintenance-container">
    <div class="logo-box">
        <h1 class="logo">TUTE<span>LARES</span></h1>
    </div>
    
    <div class="animation-box">
        <div class="hourglass"></div>
    </div>

    <h2>Estamos trabajando para ti</h2>
    <p><?= htmlspecialchars($message) ?></p>
    
    <div class="info-footer">
        <span>© <?= date('Y') ?> Tutelares. Todos los derechos reservados.</span>
    </div>
</div>

<style>
    :root {
        --accent: #FF1C5C;
        --dark: #0f0f14;
    }
    
    body {
        margin: 0;
        padding: 0;
        background: var(--dark);
        color: white;
        font-family: 'Inter', sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        overflow: hidden;
    }

    .maintenance-container {
        text-align: center;
        max-width: 500px;
        padding: 3rem;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 24px;
        backdrop-filter: blur(10px);
        animation: fadeIn 1s ease-out;
    }

    .logo {
        font-family: 'Syne', sans-serif;
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 2rem;
        letter-spacing: -1px;
    }
    .logo span { color: var(--accent); }

    .animation-box {
        margin: 2rem auto;
        display: flex;
        justify-content: center;
    }

    /* Animate Hourglass */
    .hourglass {
        position: relative;
        width: 40px;
        height: 60px;
        border: 4px solid #fff;
        border-radius: 4px;
        animation: rotate 2s linear infinite;
    }
    .hourglass:before {
        content: '';
        position: absolute;
        top: 0; left: 0;
        width: 100%; height: 50%;
        background: var(--accent);
        animation: fillTop 2s linear infinite;
    }
    .hourglass:after {
        content: '';
        position: absolute;
        bottom: 0; left: 0;
        width: 100%; height: 0;
        background: var(--accent);
        animation: fillBottom 2s linear infinite;
    }

    @keyframes rotate {
        0%, 90% { transform: rotate(0); }
        100% { transform: rotate(180deg); }
    }
    @keyframes fillTop {
        0% { height: 50%; }
        90%, 100% { height: 0; }
    }
    @keyframes fillBottom {
        0% { height: 0; }
        90%, 100% { height: 50%; }
    }

    h2 {
        font-family: 'Syne', sans-serif;
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: #fff;
    }

    p {
        color: rgba(255,255,255,0.6);
        line-height: 1.6;
        font-size: 0.95rem;
    }

    .info-footer {
        margin-top: 3rem;
        font-size: 0.75rem;
        color: rgba(255,255,255,0.3);
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
</style>
