<?php $this->headTitle('Super Administrador'); ?>

<div style="margin-bottom:1.5rem;">
    <h1 style="font-size:1.5rem;font-weight:800;">Panel Super Administrador</h1>
    <p style="color:#888;font-size:.9rem;margin-top:.2rem;">Vista global del portal TUTELARES</p>
</div>

<!-- Estadísticas globales -->
<div class="stats-grid" style="margin-bottom:2rem;">
    <div class="stat-card">
        <div class="stat-icon"><i class="bi bi-newspaper"></i></div>
        <div>
            <div class="stat-label">Total Noticias</div>
            <div class="stat-value"><?= $totalNoticias ?></div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:rgba(255,28,92,0.12);color:var(--accent);">
            <i class="bi bi-broadcast"></i>
        </div>
        <div>
            <div class="stat-label">Radio</div>
            <div class="stat-value" style="font-size:1rem;color:var(--accent);">EN VIVO</div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:rgba(0,123,255,0.1);color:#007bff;">
            <i class="bi bi-people"></i>
        </div>
        <div>
            <div class="stat-label">Usuarios del sistema</div>
            <div class="stat-value"><?= $totalUsuarios ?></div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:<?= $portalStatus === 'activo' ? 'rgba(40,167,69,0.1)' : 'rgba(255,193,7,0.1)' ?>;color:<?= $portalStatus === 'activo' ? '#28a745' : '#ffc107' ?>;">
            <i class="bi <?= $portalStatus === 'activo' ? 'bi-globe' : 'bi-pause-circle' ?>"></i>
        </div>
        <div style="flex:1;">
            <div class="stat-label">Estado del portal</div>
            <div class="stat-value" style="font-size:1rem;color:<?= $portalStatus === 'activo' ? '#28a745' : '#ffc107' ?>;">
                <?= $portalStatus === 'activo' ? '● Activo' : '● Stand By' ?>
            </div>
        </div>
    </div>
</div>

<div style="display:grid;grid-template-columns:2fr 1fr;gap:1.5rem;">

    <!-- Últimas noticias (sin acciones de edición — el super ve todo) -->
    <div class="card">
        <div class="card-header">
            <h2><i class="bi bi-newspaper" style="color:var(--accent);margin-right:.5rem;"></i> Últimas Noticias</h2>
            <div style="display:flex;gap:.5rem;">
                <a href="<?= $this->url('admin-publisher') ?>" class="btn btn-sm btn-outline">
                    <i class="bi bi-pencil"></i> Ir al Publisher
                </a>
            </div>
        </div>
        <div class="card-body" style="padding:0;">
            <table class="table">
                <thead>
                    <tr><th>ID</th><th>Título</th><th>Categoría</th><th>Fecha</th></tr>
                </thead>
                <tbody>
                    <?php if (count($noticias) > 0): ?>
                        <?php foreach (array_slice($noticias, 0, 8) as $n): ?>
                        <tr>
                            <td style="color:#999;font-size:.8rem;">#<?= $n['id'] ?></td>
                            <td><strong><?= htmlspecialchars($n['titulo']) ?></strong></td>
                            <td><span class="badge badge-pink"><?= htmlspecialchars($n['categoria']) ?></span></td>
                            <td style="font-size:.8rem;color:#999;"><?= date('d/m/Y', strtotime($n['creado_en'])) ?></td>
                        </tr>
                        <?php endforeach; ?>
                    <?php else: ?>
                        <tr><td colspan="4" style="text-align:center;padding:1.5rem;color:#999;">No hay noticias aún.</td></tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Panel lateral: accesos rápidos y usuarios -->
    <div style="display:flex;flex-direction:column;gap:1.25rem;">

        <div class="card">
            <div class="card-header"><h2><i class="bi bi-lightning" style="color:var(--accent);margin-right:.5rem;"></i> Gestión de Usuarios</h2></div>
            <div class="card-body" style="display:flex;flex-direction:column;gap:.75rem;">
                <a href="<?= $this->url('admin-super', ['action' => 'saveUsuario']) ?>" class="btn btn-pink" style="justify-content:center;">
                    <i class="bi bi-person-plus-fill"></i> Crear Nuevo Usuario
                </a>
                <a href="<?= $this->url('admin-super', ['action' => 'usuarios']) ?>" class="btn btn-outline" style="justify-content:center;">
                    <i class="bi bi-people-fill"></i> Administrar Cuentas
                </a>
            </div>
        </div>

        <div class="card">
            <div class="card-header"><h2><i class="bi bi-people" style="color:var(--accent);margin-right:.5rem;"></i> Usuarios Recientes</h2></div>
            <div class="card-body" style="display:flex;flex-direction:column;gap:.75rem;">
                <?php foreach (array_slice($usuarios, 0, 5) as $u): ?>
                <div style="display:flex;align-items:center;gap:.75rem;padding:.5rem .75rem;background:#f9f9f9;border-radius:8px;">
                    <div style="width:32px;height:32px;border-radius:50%;background:<?= $u['rol'] === 'super' ? '#111' : ($u['rol'] === 'publisher' ? 'var(--accent)' : '#007bff') ?>;color:#fff;display:flex;align-items:center;justify-content:center;font-size:.7rem;font-weight:800;flex-shrink:0;">
                        <?= strtoupper(substr($u['usuario'], 0, 2)) ?>
                    </div>
                    <div style="overflow:hidden;">
                        <div style="font-weight:600;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;"><?= htmlspecialchars($u['nombre'] ?: $u['usuario']) ?></div>
                        <div style="font-size:.75rem;color:#888;"><?= htmlspecialchars($u['usuario']) ?></div>
                    </div>
                    <span class="badge" style="margin-left:auto;<?= $u['rol'] === 'super' ? 'background:rgba(0,0,0,.08);color:#111;' : ($u['rol'] === 'publisher' ? 'background:rgba(255,28,92,0.12);color:var(--accent);' : 'background:rgba(0,123,255,.12);color:#007bff;') ?>">
                        <?= htmlspecialchars($u['rol']) ?>
                    </span>
                </div>
                <?php endforeach; ?>
            </div>
        </div>

        <div class="card">
            <div class="card-header"><h2><i class="bi bi-tools" style="color:var(--accent);margin-right:.5rem;"></i> Configuración</h2></div>
            <div class="card-body" style="text-align:center;padding:1.5rem;color:#ccc;">
                <i class="bi bi-gear" style="font-size:2.5rem;display:block;margin-bottom:.75rem;opacity:.4;"></i>
                <p style="font-size:.85rem;">Configuración avanzada próximamente.</p>
            </div>
        </div>

    </div>
</div>
