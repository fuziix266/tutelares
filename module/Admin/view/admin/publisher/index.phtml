<?php $this->headTitle('Panel Publisher'); ?>

<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:1.5rem;">
    <div>
        <h1 style="font-size:1.5rem;font-weight:800;">Panel Publisher</h1>
        <p style="color:#888;font-size:.9rem;margin-top:.2rem;">Redacción y publicación de noticias</p>
    </div>
    <a href="/medio-comunicacion/public/admin/publisher/crear" class="btn btn-primary">
        <i class="bi bi-plus-lg"></i> Nueva Noticia
    </a>
</div>

<!-- Estadísticas rápidas -->
<div class="stats-grid" style="margin-bottom:1.5rem;">
    <div class="stat-card">
        <div class="stat-icon"><i class="bi bi-newspaper"></i></div>
        <div>
            <div class="stat-label">Mis Noticias</div>
            <div class="stat-value"><?= count($noticias) ?></div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:rgba(40,167,69,0.1);color:#28a745;"><i class="bi bi-check-circle"></i></div>
        <div>
            <div class="stat-label">Publicadas</div>
            <div class="stat-value"><?= count($noticias) ?></div>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h2><i class="bi bi-newspaper" style="color:var(--accent);margin-right:.5rem;"></i> Noticias</h2>
    </div>
    <div class="card-body" style="padding:0;">
        <?php if (empty($noticias)): ?>
        <div style="padding:3rem;text-align:center;color:#999;">
            <i class="bi bi-inbox" style="font-size:2.5rem;display:block;margin-bottom:1rem;"></i>
            Aún no has publicado ninguna noticia.
            <br><br>
            <a href="/medio-comunicacion/public/admin/publisher/crear" class="btn btn-primary">Crear primera noticia</a>
        </div>
        <?php else: ?>
        <table class="table">
            <thead>
                <tr>
                    <th width="50">ID</th><th>Título</th><th>Categoría</th>
                    <th>Autor</th><th>Fecha</th><th width="130">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($noticias as $n): ?>
                <tr>
                    <td style="color:#999;font-size:.8rem;">#<?= $n['id'] ?></td>
                    <td>
                        <strong><?= htmlspecialchars($n['titulo']) ?></strong>
                        <?php if (!empty($n['resumen'])): ?>
                        <div style="font-size:.8rem;color:#999;margin-top:.2rem;"><?= htmlspecialchars(substr($n['resumen'], 0, 70)) ?>...</div>
                        <?php endif; ?>
                    </td>
                    <td><span class="badge badge-pink"><?= htmlspecialchars($n['categoria']) ?></span></td>
                    <td style="font-size:.9rem;"><?= htmlspecialchars($n['autor']) ?></td>
                    <td style="font-size:.8rem;color:#999;white-space:nowrap;">
                        <?= date('d/m/Y', strtotime($n['creado_en'])) ?>
                    </td>
                    <td>
                        <a href="/medio-comunicacion/public/admin/publisher/editar/<?= $n['id'] ?>"
                           class="btn btn-sm btn-outline" title="Editar">
                            <i class="bi bi-pencil"></i>
                        </a>
                        &nbsp;
                        <a href="/medio-comunicacion/public/admin/publisher/eliminar/<?= $n['id'] ?>"
                           class="btn btn-sm btn-danger" title="Eliminar"
                           onclick="return confirm('¿Eliminar esta noticia?')">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
        <?php endif; ?>
    </div>
</div>
